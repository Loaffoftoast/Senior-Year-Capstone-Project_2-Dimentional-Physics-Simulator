essentially right now main.py is the functioning code that like runs everything
  main.py just kind of runs everything else

library is the folder that holds all the classes and functions used in main.py
  each file's use in library is pretty self explanatory just read the title

testing currently has my math for my physics in physics.py
  right now its mostly empty due to me not doing much math yet
it also has my test work to impliment OOP (object oriented programming) into my main code because that will be very useful at some point
